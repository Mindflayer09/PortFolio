import React from 'react';

export const Terminal = ({ className }: { className?: string }) => (
  <span className={`material-symbols-outlined ${className}`}>terminal</span>
);

export const Code = ({ className }: { className?: string }) => (
  <span className={`material-symbols-outlined ${className}`}>code</span>
);

export const ArrowOutward = ({ className }: { className?: string }) => (
  <span className={`material-symbols-outlined ${className}`}>arrow_outward</span>
);

export const NorthEast = ({ className }: { className?: string }) => (
  <span className={`material-symbols-outlined ${className}`}>north_east</span>
);

export const Javascript = ({ className }: { className?: string }) => (
  <span className={`material-symbols-outlined ${className}`}>javascript</span>
);

export const DeployedCode = ({ className }: { className?: string }) => (
  <span className={`material-symbols-outlined ${className}`}>deployed_code</span>
);

export const Palette = ({ className }: { className?: string }) => (
  <span className={`material-symbols-outlined ${className}`}>palette</span>
);

export const Database = ({ className }: { className?: string }) => (
  <span className={`material-symbols-outlined ${className}`}>database</span>
);

export const CloudQueue = ({ className }: { className?: string }) => (
  <span className={`material-symbols-outlined ${className}`}>cloud_queue</span>
);

export const Mail = ({ className }: { className?: string }) => (
  <span className={`material-symbols-outlined ${className}`}>mail</span>
);
